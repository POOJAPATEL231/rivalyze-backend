# Rivalyze — backend (strict-start variant)
Initial commit = specifications and configuration ONLY. Zero application code — every line
of Python is written during CodeClash 2026. References: app/db/schema.sql (DDL),
docs/models_spec.py.txt (the typed contract, transcribed into app/models.py as the event's
first task), docs/llm_prompts.md, docs/data_dictionary.md. Run instructions arrive with the
first real commit. Team Argus · AI assistance: /DECLARATION.md.
